import styles from './FestivalSection.module.css';
import SectionWrapper from '../commons/SectionWrapper';

export default function FestivalSection() {
  return (
    <SectionWrapper>
      <p className={styles.title}>축제</p>
    </SectionWrapper>
  );
}
